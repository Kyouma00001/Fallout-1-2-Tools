# Sprint 5.1 - Visual editor scaling hotfix

Adds visual text controls:

- Width scale
- Height scale
- Letter spacing
- nearest-neighbor resizing for pixel art

Apply by copying the archive contents to the repository root, then run:

```bash
dotnet build
dotnet test
dotnet run --project src/Fallout.Tools.UI
```
